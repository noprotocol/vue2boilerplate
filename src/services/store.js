import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

// Initial state
const state = {
}

// Actions
const actions = {
}

// Mutations
const mutations = {
}

const store = new Vuex.Store({
    state,
    mutations,
    actions
})

export default store
